# AI Mode: Set to 'local' to use Mistral 7B in LM Studio or 'openai' to use OpenAI API or 'deepseek' to use DeepSeek API
AI_MODE=local

# LM Studio URL (used when AI_MODE=local)
LM_STUDIO_URL=http://localhost:1234/v1

# OpenAI API Key (used when AI_MODE=openai)
# OPENAI_API_KEY=your_openai_api_key_here

# DeepSeek API Configuration (used when AI_MODE=deepseek)
# DEEPSEEK_API_KEY=your_deepseek_api_key_here
# DEEPSEEK_MODEL=auto  # Options: 'auto', 'chat' (DeepSeek-V3), 'reasoner' (DeepSeek-R1)
# AUTO_SELECT_MODEL=True  # Set to True to intelligently choose model based on task complexity
